import { VanillaEnum } from './enum.mjs'

// Enum.One = 'try to assign something else' // it throws Cannot assign exception

console.log({ VanillaEnum }) // { VanillaEnum: { One: 1, Two: 2, Three: 3 } }
